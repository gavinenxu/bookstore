# type Post {
#   id: ID! @unique
#   published: Boolean! @default(value: "false")
#   title: String!
#   content: String!
# }

type Book {
  id: Id! @unique
  primary_isbn: String!
  authors: [Author!]!
}

type Author {
  id: Id! @unique
  role: Int!
}
